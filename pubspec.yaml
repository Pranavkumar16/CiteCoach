name: citecoach
description: "CiteCoach - Offline Document Intelligence with Evidence-Based Answers"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter

  # Icons
  cupertino_icons: ^1.0.8

  # State Management
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5

  # Navigation
  go_router: ^14.2.0

  # Database & Storage
  sqflite: ^2.3.3
  path: ^1.9.0
  path_provider: ^2.1.3
  shared_preferences: ^2.2.3

  # PDF
  syncfusion_flutter_pdfviewer: ^27.1.58
  syncfusion_flutter_pdf: ^27.1.58
  
  # OCR - On-device text recognition (Google ML Kit)
  google_mlkit_text_recognition: ^0.15.1

  # Word Documents (.doc, .docx)
  docx_to_text: ^1.0.1

  # EPUB (ebooks)
  epubx: ^4.0.0

  # Image handling for OCR (constrained by epubx ^4.0.0 dependency)
  image: ^3.3.0

  # File Handling
  file_picker: ^10.3.10

  # Voice
  speech_to_text: ^7.0.0
  flutter_tts: ^4.0.2

  # Battery & System
  battery_plus: ^7.0.0

  # SVG
  flutter_svg: ^2.0.10+1

  # Math & Utilities
  uuid: ^4.4.0
  collection: ^1.18.0
  equatable: ^2.0.5
  intl: ^0.19.0

  # HTTP for model download
  dio: ^5.4.3+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  riverpod_generator: ^2.4.0
  build_runner: ^2.4.9
  sqflite_common_ffi: ^2.3.3
  shared_preferences_platform_interface: ^2.3.2

flutter:
  uses-material-design: true
  
  assets:
    - assets/
    - assets/icons/

  fonts:
    - family: Lexend
      fonts:
        - asset: assets/fonts/Lexend-Light.ttf
          weight: 300
        - asset: assets/fonts/Lexend-Regular.ttf
          weight: 400
        - asset: assets/fonts/Lexend-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/Lexend-Bold.ttf
          weight: 700
        - asset: assets/fonts/Lexend-ExtraBold.ttf
          weight: 800
